export interface Config {
  sourcePath: string;
  journalPath: string;
  templatesPath: string;
  staticPath: string;
  pagesPath: string;
  buildPath: string;
  baseUrl: string;
  siteTitle: string;
  siteDescription: string;
  rssItemCount: number;
  rssSummaryLength: number;
}
